// gatsby-node.js
// Minimal placeholder so Netlify/Gatsby hooks that try to require this file won't fail.
// If you later confirm Gatsby is not used at all, you can remove this file and
// remove the plugin/config that requires Gatsby.

exports.createPages = async () => {
  // intentionally empty
};
